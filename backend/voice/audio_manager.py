"""Audio preprocessing and output helpers."""


class AudioManager:
    """Normalizes, chunks, and validates audio streams."""

    async def normalize(self, audio_path: str) -> str:
        # TODO: Apply format conversion and level normalization.
        return audio_path
